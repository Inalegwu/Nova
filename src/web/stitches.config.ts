import { createStitches } from "@stitches/react";

export const { styled, css, globalCss, theme } = createStitches({
  theme: {
    colors: {},
    fonts: {
      body: "Font",
      number: "Count",
    },
  },
});
